# agents-cli rules — single default preset
#
# Includes all subrules from `<repo>/rules/subrules/`.

presets:
  default:
    subrules:
      - core-hard-lines
      - workflow-proactive
      - testing-strict
      - git
      - operational
      - conventions
      - linear-tickets
      - parallel-teams
      - tech-stack
